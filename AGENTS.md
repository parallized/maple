# Maple Agent Engineering Rules

1. 架构优先：按职责拆分模块与组件，禁止把大量业务逻辑堆在单一文件中形成“意大利面条代码”。
2. 直接实现：用户明确提出的要求必须直接落地，禁止以提示、手动输入、模拟流程等方式做兜底替代。
3. 图标规范：前端图标统一使用 Iconify 的 `mingcute` 图标集。永远不要出现 sparkles 之类的 “AI 闪光” 图标（如 `mingcute:sparkles-line`），人机感太重。
4. 文案规范：面向用户的界面文案必须产品化、克制、可发布，禁止输出研发口吻或 “莫名其妙” 的占位文本。
5. 样式规范：默认采用 Notion 风格的视觉语言（留白、弱分隔、低噪声配色）
6. 阴影规范：深色 / 暗色背景上的浮层（tooltip、popover、卡片等）禁止灰 / 黑色 box-shadow，会产生阶层式脏块；层级分隔默认用细描边，如需浮起感则用极轻微的品牌紫（`--color-primary` 低透明度）阴影。

## Worker

- 如果你是 kimi 模型，你负责完成前端，以绝佳设计师能力完成 ui ux 升级
- 如果你是 codex 模型，负责完成后端数据库等看不到的开发、扩展、升级，严禁去做前端

## Coding

- 使用 bun monorepo + ts
- 前端用 react + tailwind，样式用 "flex flex-col gap-2" 这种 className 来表达而不是创建新 .class modifier
- 后端用高性能大并发的 ts 框架，数据库使用 sqlite
- 除非我要求，否则不允许往 package.json 加新的 scripts

## Publish

- 在接收到发版指令之后执行以下工序
  1. 简明告诉用户，上次版本号到这次预计更新的版本号 + 上次更新时间 + 这次更新内容，不要多一个字废话，不解释细节
  2. 等待用户评价：是否批准，以及要做哪些调整，比如这次是否要迈大步更新一个大版本号等，你也可以给出相关建议
  3. 执行发版，打 tag，如果是大版本号也就是小数点前一位的更新，给一个文艺 / 可爱命名，像 Android 那样
  4. 将这个版本更新的内容简述到版本历史文件，原则上每一行描述不超过 30 字符

{
  "name": "@maple/web",
  "private": true,
  "version": "0.1.3",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"lint not configured for @maple/web\""
  },
  "dependencies": {
    "@maple/agent-runtime": "workspace:*",
    "@maple/kanban-core": "workspace:*"
  }
}
